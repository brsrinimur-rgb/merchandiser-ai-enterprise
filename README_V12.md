# v12 Integration Hub Upgrade

- Structured `/api/integration-hub/connectors` response with complete OpenAPI schemas
- Connector readiness, authentication type, capabilities and operational metrics
- Pull preview/live controls with top/filter options
- Controlled push with explicit `PUSH` confirmation
- Searchable audit logs
- D365 secrets remain server-side
- Sandbox connector for safe testing
